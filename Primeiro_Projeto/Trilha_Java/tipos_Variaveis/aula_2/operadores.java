package Primeiro_Projeto.Trilha_Java.tipos_Variaveis.aula_2;

public class operadores {
    public static void main(String[] args) {
        /*
         * Operador de atribuição ( = )
         * 
         * Para atribuir valor a uma variável
         */

        /*
         * Operadores Aritméticos ( + - * / %)
         * 
         * + pode ser utilizado para concatenção também
         */

        /*
         * Operador Unário ( valor positivo (+), valor negativo (-), incremento ++,
         * decremento --, ! inverso )
         */

        /*
         * Operador Ternário ( ? e :)
         * ? indica expressão condicional caso a expressão seja verdadeira
         * : indica expressão condicional caso a expressão seja falsa
         */

        /*
         * Operadores Relacionais ( == igualdade != diferença > maior que
         * < menor que)
         */

        /*
         * Operadores Lógicos (&& = e || = ou)
         */
    }
}
